import { LAB_CATALOG, validateCfdi40Xsd, validateLabXml, type LabReport } from "@/lib/lab";
import { ArrowLeft, FileCheck2, FlaskConical, LoaderCircle, Upload } from "lucide-react";
import { type ChangeEvent, useRef, useState } from "react";
import { Link } from "wouter";

export default function Lab() {
  const input = useRef<HTMLInputElement>(null); const [report, setReport] = useState<LabReport | null>(null); const [xsd, setXsd] = useState<{ valid: boolean; errors: { message: string; line: number | null }[] } | null>(null); const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(false);
  const load = async (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; event.target.value = ""; if (!file) return; setBusy(true); setMessage(null); setReport(null); setXsd(null); try { const xml = await file.text(); const next = validateLabXml(xml); setReport(next); if (next.root === "Comprobante") setXsd(await validateCfdi40Xsd(xml)); } catch (error) { setMessage(error instanceof Error ? error.message : "No fue posible validar el XML."); } finally { setBusy(false); } };
  return <main className="min-h-screen bg-[#f5f1e8] px-5 py-8 text-[#171611] sm:px-8 lg:px-12"><div className="mx-auto max-w-5xl"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[#9b571f]"><ArrowLeft size={16} /> Volver a Consulta CFDI</Link><section className="mt-10 rounded-[2rem] border border-[#d9d2c4] bg-[#fffdf8] p-6 shadow-[0_24px_80px_rgba(56,40,21,0.10)] sm:p-10"><div className="grid gap-8 lg:grid-cols-[1fr_.72fr]"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#b76421]">Laboratorio local</p><h1 className="mt-3 font-[var(--font-display)] text-5xl tracking-[-.05em]">Fixtures, reglas y XSD.</h1><p className="mt-5 max-w-2xl leading-7 text-[#696459]">Valida XML sintético y comprobantes CFDI 4.0 en este navegador. No consulta al SAT, no guarda archivos y no emite conclusiones fiscales.</p></div><div className="rounded-2xl bg-[#171611] p-5 text-[#f6f2e9]"><FlaskConical size={22} className="text-[#f5b46f]" /><p className="mt-4 text-sm font-semibold">{LAB_CATALOG.roots.length} raíces educativas · {LAB_CATALOG.ruleCodes.length} reglas</p><p className="mt-2 text-xs leading-5 text-[#d7d0c5]">Incluye el perfil XSD CFDI 4.0 offline y los fixtures migrados desde el laboratorio original.</p><Link href="/laboratorio/cotizador" className="mt-4 inline-flex text-sm font-semibold text-[#f5b46f]">Abrir cotizador sintético →</Link></div></div><div className="mt-9 rounded-2xl border border-dashed border-[#cbbda8] bg-[#faf6ef] p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="font-semibold">Selecciona un XML para validar localmente</p><p className="mt-1 text-sm text-[#70695c]">Comprobante activa reglas educativas y la validación estructural XSD.</p></div><button onClick={() => input.current?.click()} disabled={busy} className="inline-flex items-center justify-center gap-2 rounded-full bg-[#171611] px-5 py-3 text-sm font-semibold text-white disabled:opacity-60">{busy ? <LoaderCircle className="animate-spin" size={17} /> : <Upload size={17} />}{busy ? "Validando…" : "Seleccionar XML"}</button></div><input ref={input} onChange={load} type="file" accept=".xml,application/xml,text/xml" className="sr-only" />{message && <p className="mt-5 rounded-xl bg-[#fde6de] p-4 text-sm text-[#9e352c]">{message}</p>}</div>{report && <section className="mt-8 grid gap-5 lg:grid-cols-2"><article className="rounded-2xl border border-[#d9d2c4] p-5"><div className="flex items-center gap-3"><FileCheck2 className="text-[#26775a]" /><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#777164]">Reglas educativas</p><h2 className="font-[var(--font-display)] text-2xl">{report.root}</h2></div></div><p className="mt-5 text-sm text-[#696459]">{report.issues.length ? `${report.issues.length} observación(es) local(es).` : "Sin observaciones para las reglas de este laboratorio."}</p><div className="mt-4 space-y-3">{report.issues.map(item => <div key={`${item.code}-${item.fragment}`} className="rounded-xl bg-[#f4eee4] p-3"><p className="font-mono text-xs font-bold text-[#9b571f]">{item.code}</p><p className="mt-1 text-sm font-semibold">{item.message}</p><p className="mt-1 font-mono text-xs text-[#777164]">{item.fragment}</p></div>)}</div></article>{xsd && <article className="rounded-2xl border border-[#d9d2c4] p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#777164]">Perfil CFDI 4.0 XSD</p><h2 className="mt-2 font-[var(--font-display)] text-2xl">{xsd.valid ? "Estructura válida" : "Errores estructurales"}</h2><p className="mt-3 text-sm text-[#696459]">{xsd.valid ? "El esquema local aceptó la estructura. No valida sellos, catálogos, PAC ni aceptación SAT." : "El esquema local encontró diferencias estructurales."}</p><div className="mt-4 space-y-2">{xsd.errors.map(error => <p key={`${error.line}-${error.message}`} className="rounded-xl bg-[#f4eee4] p-3 text-sm">{error.line ? `Línea ${error.line}: ` : ""}{error.message}</p>)}</div></article>}</section>}</section></div></main>;
}
